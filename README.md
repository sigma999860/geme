# game
<!DOCTYPE html><html lang="nl"><head> <meta charset="UTF-8" /> <meta name="viewport" content="width=device-width, initial-scale=1.0"/> <title>Endless Runner Spel</title> <style> * { box-sizing: border-box; font-family: 'Segoe UI', sans-serif; } body { margin: 0; padding: 0; display: flex; justify-content: center; align-items: center; height: 100vh; background: linear-gradient(to bottom right, #74ebd5, #ACB6E5); flex-direction: column; } canvas { background-color: #3498db; border: 3px solid #2c3e50; border-radius: 10px; box-shadow: 0 4px 10px rgba(0,0,0,0.3); } .game-over { position: absolute; top: 20%; left: 50%; transform: translateX(-50%); font-size: 3rem; color: #e74c3c; font-weight: bold; display: none; animation: pop 0.5s ease-out; } @keyframes pop { 0% { transform: scale(0.5) translateX(-50%); opacity: 0; } 100% { transform: scale(1) translateX(-50%); opacity: 1; } } .top-bar { position: absolute; top: 10px; left: 0; right: 0; display: flex; justify-content: space-between; padding: 0 20px; align-items: center; z-index: 2; } .score { font-size: 1.8rem; color: #f1c40f; text-shadow: 1px 1px 2px #000; display: flex; align-items: center; gap: 8px; } .right-controls { display: flex; gap: 10px; } .button { padding: 8px 16px; font-size: 1rem; background-color: #2980b9; color: white; border: none; border-radius: 8px; cursor: pointer; box-shadow: 0 2px 5px rgba(0,0,0,0.2); transition: background-color 0.3s; display: flex; align-items: center; gap: 6px; } .button:hover { background-color: #1c5980; } .icon { font-weight: bold; } </style></head><body> <canvas id="gameCanvas" width="800" height="400"></canvas> <div class="game-over" id="gameOverText">Game Over!</div> <!-- Bovenste balk met score en knoppen --> <div class="top-bar"> <div class="score" id="score"><span class="icon">🏆</span> Score: 0</div> <div class="right-controls"> <button class="button" id="pauseButton"><span class="icon">⏸️</span> Pauze</button> <button class="button" id="restartButton" style="display:none;"><span class="icon">🔄</span> Opnieuw</button> </div> </div> <script> const canvas = document.getElementById('gameCanvas'); const ctx = canvas.getContext('2d'); const gameOverText = document.getElementById('gameOverText'); const scoreText = document.getElementById('score'); const pauseButton = document.getElementById('pauseButton'); const restartButton = document.getElementById('restartButton'); const playerWidth = 30; const playerHeight = 60; const groundHeight = 100; const jumpHeight = 15; const gravity = 0.6; const moveSpeed = 5; let playerX = 50; let playerY = canvas.height - groundHeight - playerHeight; let playerSpeedY = 0; let isJumping = false; let score = 0; let obstacles = []; let gameRunning = true; let lastObstacleX = 0; let isPaused = false; // De speler object const player = { x: playerX, y: playerY, width: playerWidth, height: playerHeight, color: '#f39c12', }; // De deeltjes die op de grond vallen wanneer de speler landt let particles = []; let jumpAnimationProgress = 0; // Tekent de speler en zorgt voor de animatie van het springen function drawPlayer() { // Schaduw onder de speler const maxY = canvas.height - groundHeight - playerHeight; const shadowScale = Math.max(0.3, 1 - (maxY - player.y) / 100); ctx.fillStyle = 'rgba(0, 0, 0, 0.3)'; ctx.beginPath(); ctx.ellipse(player.x + player.width / 2, canvas.height - groundHeight + 5, 20 * shadowScale, 8 * shadowScale, 0, 0, Math.PI * 2); ctx.fill(); // Spring animatie: stretch/squash effect const jumpSquish = 1 - jumpAnimationProgress * 0.3; const jumpLift = jumpAnimationProgress * 10; // Lichaam tekenen ctx.fillStyle = '#f39c12'; ctx.fillRect(player.x + 10, player.y + 20 + jumpLift, 10, 30 * jumpSquish); // Hoofd tekenen ctx.fillStyle = '#f1c40f'; ctx.beginPath(); ctx.arc(player.x + 15, player.y + 10 + jumpLift - jumpAnimationProgress * 5, 10, 0, Math.PI * 2); ctx.fill(); // Armen tekenen ctx.fillStyle = '#f39c12'; ctx.fillRect(player.x, player.y + 20 + jumpLift, 10, 5); ctx.fillRect(player.x + 20, player.y + 20 + jumpLift, 10, 5); // Benen tekenen ctx.fillStyle = '#e74c3c'; ctx.fillRect(player.x + 10, player.y + 50 + jumpLift, 5, 10); ctx.fillRect(player.x + 15, player.y + 50 + jumpLift, 5, 10); } // Functie om een obstakel te maken function createObstacle() { const obstacleWidth = 30 + Math.random() * 20; const obstacleHeight = 30 + Math.random() * 20; const obstacleX = lastObstacleX + 200 + Math.random() * 50; const obstacleY = canvas.height - groundHeight - obstacleHeight; lastObstacleX = obstacleX; return { x: obstacleX, y: obstacleY, width: obstacleWidth, height: obstacleHeight }; } // Start het spel function startGame() { score = 0; obstacles = []; lastObstacleX = 0; gameRunning = true; isPaused = false; gameOverText.style.display = 'none'; player.y = canvas.height - groundHeight - playerHeight; playerSpeedY = 0; scoreText.innerHTML = `<span class="icon">🏆</span> Score: ${score}`; restartButton.style.display = 'none'; pauseButton.textContent = '⏸️ Pauze'; mainLoop(); } // Hoofdlus van het spel function mainLoop() { if (!gameRunning || isPaused) return; ctx.clearRect(0, 0, canvas.width, canvas.height); if (isJumping) { playerSpeedY -= gravity; player.y -= playerSpeedY; jumpAnimationProgress = Math.min(1, (canvas.height - groundHeight - player.y) / 100); if (player.y >= canvas.height - groundHeight - playerHeight) { player.y = canvas.height - groundHeight - playerHeight; isJumping = false; jumpAnimationProgress = 0; // Stofwolkjes bij landing for (let i = 0; i < 5; i++) { particles.push({ x: player.x + 15 + Math.random() * 10 - 5, y: canvas.height - groundHeight, vx: Math.random() * 2 - 1, vy: -Math.random() * 1.5, alpha: 1, size: 3 + Math.random() * 2 }); } } } // Obstakels maken if (Math.random() < 0.02) { obstacles.push(createObstacle()); } // Obstakels bewegen for (let i = 0; i < obstacles.length; i++) { obstacles[i].x -= 5; if (obstacles[i].x + obstacles[i].width < 0) { obstacles.splice(i, 1); score++; scoreText.innerHTML = `<span class="icon">🏆</span> Score: ${score}`; i--; } } // Botsingen controleren for (let i = 0; i < obstacles.length; i++) { const obs = obstacles[i]; if ( player.x + player.width > obs.x && player.x < obs.x + obs.width && player.y + player.height > obs.y ) { gameRunning = false; gameOverText.style.display = 'block'; restartButton.style.display = 'inline-block'; pauseButton.style.display = 'none'; } } // De deeltjes die op de grond vallen for (let i = particles.length - 1; i >= 0; i--) { const particle = particles[i]; particle.x += particle.vx; particle.y += particle.vy; particle.alpha -= 0.05; particle.vy += gravity; if (particle.alpha <= 0) { particles.splice(i, 1); } ctx.fillStyle = `rgba(200, 200, 200, ${particle.alpha})`; ctx.beginPath(); ctx.arc(particle.x, particle.y, particle.size, 0, Math.PI * 2); ctx.fill(); } // Speler tekenen drawPlayer(); // Obstakels tekenen ctx.fillStyle = '#c0392b'; for (let obs of obstacles) { ctx.fillRect(obs.x, obs.y, obs.width, obs.height); } // De hoofdgame-loop voortzetten if (gameRunning) { requestAnimationFrame(mainLoop); } } // De speler springen function jump() { if (!isJumping) { isJumping = true; playerSpeedY = jumpHeight; } } // Toetsenbord-eventlistener document.addEventListener('keydown', (event) => { if (event.key === ' ' || event.key === 'ArrowUp') jump(); if (event.key === 'ArrowLeft') player.x = Math.max(0, player.x - moveSpeed); if (event.key === 'ArrowRight') player.x = Math.min(canvas.width - player.width, player.x + moveSpeed); }); // Pauze knop pauseButton.addEventListener('click', () => { isPaused = !isPaused; pauseButton.textContent = isPaused ? '▶️ Hervat' : '⏸️ Pauze'; if (!isPaused) mainLoop(); }); // Herstart knop restartButton.addEventListener('click', startGame); startGame(); </script></body></html>
